<h1>Listing klass_result_sets</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @klass_result_sets.each do |klass_result_set| %>
      <tr>
        <td><%= link_to 'Show', klass_result_set %></td>
        <td><%= link_to 'Edit', edit_klass_result_set_path(klass_result_set) %></td>
        <td><%= link_to 'Destroy', klass_result_set, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Klass result set', new_klass_result_set_path %>
