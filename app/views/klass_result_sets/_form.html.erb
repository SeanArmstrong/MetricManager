<%= form_for(@klass_result_set) do |f| %>
  <% if @klass_result_set.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@klass_result_set.errors.count, "error") %> prohibited this klass_result_set from being saved:</h2>

      <ul>
      <% @klass_result_set.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
