<%= form_for(@methood) do |f| %>
  <% if @methood.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@methood.errors.count, "error") %> prohibited this methood from being saved:</h2>

      <ul>
      <% @methood.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
